<%= form_for @profesiones_usuario, :html => { :class => "form-horizontal profesiones_usuario" } do |f| %>

  <% if @profesiones_usuario.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@profesiones_usuario.errors.count, "error") %> prohibited this profesiones_usuario from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @profesiones_usuario.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :usuario_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :usuario_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:usuario_id) %>
  </div>
  <div class="form-group">
    <%= f.label :profesion_id, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :profesion_id, :class => 'form-control' %>
    </div>
    <%=f.error_span(:profesion_id) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                profesiones_usuarios_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
