<%- model_class = ReferenciasPersonale -%>
<button type="button" class="close" data-dismiss="modal" aria-hidden="true" onclick="$.lazybox.close()">&times;</button>
<div class="page-header">
  <h2>Referencia Personal:<%= @referencias_personale.id %></h2>
  <table class="table table-bordered table-hover" >
    <thead>
      <tr class="info">
        <th>Usuario</th>
        <th>Telefono</th>
        <th>Profesión</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><p><%= @referencias_personale.nombre %></p></td>
        <td><p><%= @referencias_personale.telefono %></p></td>
        <td><p><%= @referencias_personale.profesion %></p></td> 
      </tr>
    </tbody>
  </table>
  <div class="modal-footer">
    <a href="#" class="btn btn-primary" onclick="$.lazybox.close()">Cerrar</a>
  </div>
</div>